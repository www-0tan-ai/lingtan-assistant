{
  "name": "0tan-electron",
  "version": "0.1.0",
  "private": true,
  "description": "0tan desktop shell (Electron + Python tui_gateway WebSocket)",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "pack:win": "electron-builder --win portable --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^34.2.0",
    "electron-builder": "^25.1.8"
  }
}
