# HermesDesk — 首次运行会复制为 hermes_data/.env；按需取消注释并填写。
#
# 默认 config 指向本机 Ollama (http://127.0.0.1:11434/v1)，一般无需任何密钥即可对话。
#
# ── 若改为 OpenAI 或 OpenAI 兼容云服务，请在 config.yaml 里设置对应的 provider/base_url，
#    并在此填写密钥（示例）：
# OPENAI_API_KEY=
# （部分代理需要时可设 OPENAI_BASE_URL=，通常更推荐写在 config.yaml 的 model.base_url）
#
# 其他提供商所需变量见仓库 hermes_cli/config.py 中 OPTIONAL_ENV_VARS。
